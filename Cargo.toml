[package]
name = "rs-cmish"
version = "0.1.0"

description='''
Content Management Interactive Shell: a CLI for your CMIS archive.
'''

edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
